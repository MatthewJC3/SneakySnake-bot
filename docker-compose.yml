---
version: "2.1"
services:
  discord-bot:
    image: sneakysnake:latest
    container_name: sneakysnake
    volumes:
      - .:/bot
    environment:
      - DISCORD_TOKEN=
    restart: unless-stopped